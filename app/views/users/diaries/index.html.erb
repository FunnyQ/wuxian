<!-- index.html -->
<%= render :partial => "partials/user_cover", locals: {user: @user} %>

<div class="user-main-content container">

  <!-- 側邊資訊欄位 -->
  <div class="user-sidebar">

    <!-- 使用者資料卡，包含社交按鈕 -->
    <%= render :partial => "partials/user_title_card", locals: {user: @user} %>

    <!-- 最新網誌 -->
    <%= render :partial => "partials/user_diary_latest_list", locals: {user: @user} %>

    <!-- 最新留言 -->
    <%= render :partial => "partials/user_diary_comments_list", locals: {user: @user} %>

    <!-- 廣告欄位 -->
    <%= render :partial => "partials/ad_widget" %>

  </div>

  <!-- 主要資料欄位 -->
  <div class="user-main">

    <%= render :partial => "partials/user_content_tabs", locals: {user: @user} %>

    <!-- 日記區塊 -->
    <div class="user-diaries-wrapper">

      <!-- 相簿選單欄位 -->
      <div class="album-create-options">

        <!-- 新增日記 -->
        <%= link_to new_user_diary_path, class: "album-new" do %>
          <i class="fa fa-plus"></i> 新增日記
        <% end %>
      </div>
      <% 5.times.each do |d| %>
        <%= render :partial => "partials/user_diary", locals: {user: @user, id: 1} %>
      <% end %>
    </div>

  </div>
</div>
